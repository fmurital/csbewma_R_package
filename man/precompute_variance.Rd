% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{precompute_variance}
\alias{precompute_variance}
\title{Precompute variance vector for all time points}
\usage{
precompute_variance(lambda, max_t, converge_t = 500)
}
\arguments{
\item{lambda}{Smoothing parameter for EWMA}

\item{max_t}{Maximum time point to precompute variance for}

\item{converge_t}{Time point after which variance is set to 1 (asymptotic)}
}
\value{
A numeric vector of length max_t containing variance at each time point
}
\description{
This function precomputes the exact variance for all time points from 1 to
max_t. For efficiency, it computes exact variances up to a convergence
threshold (converge_t) and then sets remaining values to 1 (asymptotic).
Based on the derivation, variance reaches 99\% of asymptotic value by t=227,
so converge_t = 500 is safe and efficient.
}
\examples{
# Precompute variance for lambda = 0.175, up to t = 1000
var_cache <- precompute_variance(lambda = 0.175, max_t = 1000, converge_t = 500)

}
